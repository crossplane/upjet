apiVersion: test.com/v1alpha1
kind: MyResource
metadata:
  name: my-resource
  namespace: upbound-system
spec:
  compositionRef:
    name: example-migrated
  parameters:
    region: us-west-1
    tagValue: demo-test
